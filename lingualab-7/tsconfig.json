/** @type {import('next').NextConfig} */
const nextConfig = {
  typescript: {
    // 忽略 TypeScript 编译错误
    ignoreBuildErrors: true,
  },
  eslint: {
    // 忽略 ESLint 校验错误
    ignoreDuringBuilds: true,
  },
};

export default nextConfig;
