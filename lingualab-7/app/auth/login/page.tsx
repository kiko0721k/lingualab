"use client";import {useState} from "react";import {createClient} from "@/lib/supabase/client";import {useRouter} from "next/navigation";import Link from "next/link";
export default function Login(){const[e,setE]=useState('');const[p,setP]=useState('');const[err,setErr]=useState('');const r=useRouter();async function go(x:React.FormEvent){x.preventDefault();const {error}=await createClient().auth.signInWithPassword({email:e,password:p});if(error)setErr(error.message);else r.push('/dashboard')}return <div className="shell" style={{paddingTop:70}}><div className="card" style={{maxWidth:520,margin:'auto'}}><div className="eyebrow">Account</div><h2>登录 LinguaLab</h2><form className="form" onSubmit={go}><input type="email" placeholder="邮箱" value={e} onChange={x=>setE(x.target.value)} required/><input type="password" placeholder="密码" value={p} onChange={x=>setP(x.target.value)} required/><button className="btn primary">登录</button>{err&&<div className="alert">{err}</div>}</form><p className="muted">没有账号？ <Link href="/auth/signup">注册</Link></p></div></div>}
