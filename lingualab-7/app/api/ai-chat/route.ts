import {NextResponse} from 'next/server';import OpenAI from 'openai';export async function POST(req:Request){if(!process.env.OPENAI_API_KEY)return NextResponse.json({error:'尚未配置 OPENAI_API_KEY'},{status:503});const{input,mode='chat'}=await req.json();const c=new OpenAI({apiKey:process.env.OPENAI_API_KEY});const system=mode==='speaking-feedback'?'You are an English-major speaking coach. Give concise Chinese feedback on the learner transcript: clarity, grammar, vocabulary, naturalness, and 3 improved sentences. Do not invent pronunciation scores from text alone.':'You are LinguaLab, an English tutor for university English majors. Explain clearly with examples and distinguish verified etymology from memory tricks.';const r=await c.responses.create({model:process.env.OPENAI_TEXT_MODEL||'gpt-5.6',input:[{role:'system',content:system},{role:'user',content:String(input||'')}]});return NextResponse.json({text:r.output_text})}
