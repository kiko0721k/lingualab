import {NextResponse} from 'next/server';import OpenAI from 'openai';export async function POST(req:Request){if(!process.env.OPENAI_API_KEY)return NextResponse.json({error:'尚未配置 OPENAI_API_KEY'},{status:503});const{text}=await req.json();const c=new OpenAI({apiKey:process.env.OPENAI_API_KEY});const a=await c.audio.speech.create({model:process.env.OPENAI_TTS_MODEL||'gpt-4o-mini-tts',voice:'marin',input:String(text||''),response_format:'mp3'});return new NextResponse(await a.arrayBuffer(),{headers:{'Content-Type':'audio/mpeg'}})}
