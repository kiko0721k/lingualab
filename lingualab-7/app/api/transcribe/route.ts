import {NextResponse} from 'next/server';import OpenAI from 'openai';export async function POST(req:Request){if(!process.env.OPENAI_API_KEY)return NextResponse.json({error:'尚未配置 OPENAI_API_KEY'},{status:503});const f=(await req.formData()).get('file');if(!(f instanceof File))return NextResponse.json({error:'缺少音频文件'},{status:400});const c=new OpenAI({apiKey:process.env.OPENAI_API_KEY});const r=await c.audio.transcriptions.create({file:f,model:process.env.OPENAI_TRANSCRIBE_MODEL||'gpt-4o-mini-transcribe',language:'en'});return NextResponse.json({text:r.text})}
